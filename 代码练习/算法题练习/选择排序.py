#
# # 选择排序
# def selectsort(list):
#     n = len(list)
#     for j in range(n-1):  # 循环比较的次数
#         min_index = j
#         for i in range(j+1,n):
#             if list[min_index] > list[i]:
#                 min_index = i
#         list[min_index],list[j] = list[j],list[min_index]
#
#             #假设j位置的值为变量min，依次与遍历的i位置的值进行比较，如果i位置的值小，则将此时的i位置设定为min,
#             # 下次循环如果出现新的最小值则重复上边操作
#
# alist = [15,41,51,741,5,474,91,4]
# selectsort(alist)
# print(alist)












